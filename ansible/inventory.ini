# Inventory: a lista de maquinas que o Ansible gerencia.
# Aqui so tem uma -- a propria VM. Como o Ansible, o Docker, o kind e o kubectl
# estao TODOS nesta mesma maquina, a gente diz "conecte localmente" em vez de
# abrir um SSH pra si mesmo.
[local]
localhost ansible_connection=local
